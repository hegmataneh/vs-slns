#define Uses_M_showMsg


#include "Protocol_Bridge.use"
#include "Protocol_Bridge.use"

#include "general.h"

#include "Protocol_Bridge.inc"

#if !defined(__COMPILE) && defined(__PROJECT_NAME__) && __PROJECT_NAME__ == Protocol_Bridge // in each project must be at last  order

#define __FORCE_INTELLISENSE_ASSIST
#include <d:/My project/General projects/general/header/IntellisenseAssist.h> // should be at the end of any include to correct Intellisense bug

#endif
